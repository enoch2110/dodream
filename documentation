REQUIRED PACKAGES

1. south (sudo pip install south)
2. django rest framework (sudo pip install djangorestframework)
3. django filter(sudo pip install django-filter)
4. python-dateutil (pip install python-dateutil)